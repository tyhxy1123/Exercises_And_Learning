#include <stdio.h>

struct House
{
    char door[10];
};

int main(void)
{
    struct House h1, h2;
    h1.door = {'a','b','c'};
    for(int i = 0; i < 3; i++)
        {
	    printf(hi.door[i]);
        } 
}
